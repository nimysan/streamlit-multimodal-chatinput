import React from "react"
import ReactDOM from "react-dom"
import MultiModalInputComponent from "./MultiModalInputComponent"

ReactDOM.render(
  <React.StrictMode>
    <MultiModalInputComponent />
  </React.StrictMode>,
  document.getElementById("root")
)
